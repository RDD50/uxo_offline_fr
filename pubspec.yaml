name: uxo_offline_fr
description: Application hors ligne de consultation UXO personnelle.
publish_to: "none"

version: 1.1.0+4

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
